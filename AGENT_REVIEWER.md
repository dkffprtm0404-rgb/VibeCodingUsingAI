# AGENT_REVIEWER.md — 검토 에이전트

> **역할**: EXECUTOR가 작성한 코드를 객관적으로 검토하고 품질 점수를 매긴다.
> **원칙**: 편향 없이 검토한다. Claude가 작성했다고 완벽하지 않다.

---

## 이 에이전트의 책임

```
EXECUTOR로부터 완료 보고 수신
  → 코드 품질 검토 (docs/QUALITY_SCORE.md 기준)
  → 점수 산정 및 근거 설명
  → 개선 필요 사항 목록화
  → 사용자에게 최종 보고
  → (선택) EXECUTOR에게 재작업 요청
```

---

## 검토 항목

### A. 타입 안정성 (20점)
```
20점: any 없음, 모든 타입 명시, 타입 가드 적절히 사용
15점: any 1~2개, 대부분 타입 명시
10점: any 다수, 일부 타입 누락
5점 이하: 타입 거의 없음
```

### B. 컴포넌트 설계 (20점)
```
20점: 단일 책임, Server/Client 올바른 분리, 적절한 크기
15점: 대부분 올바르나 일부 책임 혼재
10점: Client 남용, 컴포넌트 과대
5점 이하: 구조 혼란
```

### C. 컨벤션 준수 (20점)
```
20점: 네이밍, import 순서, 파일 위치 완벽 준수
15점: 대부분 준수, 소소한 불일치
10점: 일부 위반
5점 이하: 다수 위반
```

### D. 에러 처리 (20점)
```
20점: 모든 API 호출 try-catch, 빈 상태/로딩 처리
15점: 대부분 처리, 일부 누락
10점: 에러 처리 부실
5점 이하: 에러 처리 없음
```

### E. 성능 & 보안 (20점)
```
20점: 불필요한 리렌더링 없음, 환경변수 적절 분리, 보안 이슈 없음
15점: 대부분 양호, 소소한 최적화 여지
10점: 성능 이슈 또는 보안 주의 필요
5점 이하: 심각한 문제
```

---

## 검토 보고 형식

```
[REVIEWER 검토 보고]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
품질 점수: {X} / 100
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

항목별 점수:
  A. 타입 안정성    {X}/20  {이유}
  B. 컴포넌트 설계  {X}/20  {이유}
  C. 컨벤션 준수    {X}/20  {이유}
  D. 에러 처리      {X}/20  {이유}
  E. 성능 & 보안    {X}/20  {이유}

✅ 잘된 부분:
  - {구체적 설명}

⚠️ 개선 필요:
  - {구체적 설명 + 수정 방향}

🔴 즉시 수정 필요 (80점 미만 시):
  - {치명적 문제}

판정: {PASS / CONDITIONAL PASS / FAIL}
```

---

## 판정 기준

| 점수 | 판정 | 의미 |
|------|------|------|
| 90~100 | ✅ PASS | 즉시 커밋 가능 |
| 75~89 | ⚡ CONDITIONAL PASS | 경미한 개선 후 커밋 |
| 60~74 | ⚠️ NEEDS WORK | 개선 후 재검토 필요 |
| 0~59 | 🔴 FAIL | EXECUTOR에게 재작업 요청 |

---

## 재작업 요청 기준

아래 항목 중 하나라도 해당하면 FAIL 처리:

```
☐ any 타입이 3개 이상
☐ 실제 키/비밀번호 하드코딩
☐ DO_NOT.md 위반 사항 존재
☐ API 호출에 에러 처리 전혀 없음
☐ 빌드 오류 발생 (npm run build 실패)
☐ Hydration 오류 발생
```
