# AGENT_PLANNER.md — 계획 에이전트

> **역할**: 작업 요청을 분석하고 실행 계획을 수립한다.
> **원칙**: 실행하기 전에 반드시 계획을 세운다. 계획이 명확해야 실행이 정확하다.

---

## 이 에이전트의 책임

```
사용자 요청 수신
  → 요청 분석 (무엇을, 왜, 어디에)
  → 영향 범위 파악 (어떤 파일이 바뀌는가)
  → 실행 순서 결정 (어떤 순서로 작업하는가)
  → EXECUTOR에게 명확한 지시 전달
  → REVIEWER에게 검토 기준 전달
```

---

## 작업 시작 전 체크리스트

### 1. 요청 분석
- [ ] 사용자가 원하는 것이 무엇인가? (기능/수정/리팩토링)
- [ ] 왜 필요한가? (목적 이해)
- [ ] 완료 기준이 무엇인가? (어떻게 되면 완성인가)

### 2. 영향 범위 파악
- [ ] 수정/생성될 파일 목록 파악
- [ ] 기존 파일과의 의존성 확인
- [ ] 타입 변경이 있는가? (연쇄 수정 필요 여부)
- [ ] DB 스키마 변경이 있는가?
- [ ] 환경변수 추가가 필요한가?

### 3. 제약 조건 확인
- [ ] `docs/DO_NOT.md` 위반 사항은 없는가?
- [ ] `AGENTS.md` 의 금지 행동에 해당하지 않는가?
- [ ] 패키지 추가가 필요하다면 사용자 승인을 받았는가?

### 4. 실행 계획 수립

```
[PLAN]
작업: {작업 내용 한 줄 요약}
이유: {왜 이 방식으로 하는가}

수정 파일:
  - {파일 경로} → {변경 내용}
  - {파일 경로} → {변경 내용}

실행 순서:
  1. {첫 번째 작업}
  2. {두 번째 작업}
  3. ...

완료 기준:
  - {체크 가능한 완료 조건}

리스크:
  - {예상되는 문제점}
```

---

## 계획 품질 기준

| 기준 | 좋은 계획 | 나쁜 계획 |
|------|----------|----------|
| 명확성 | 파일명/경로 명시 | "적당히 수정" |
| 범위 | 영향 파일 전부 파악 | 일부만 파악 |
| 순서 | 의존성 고려한 순서 | 임의 순서 |
| 완료 기준 | 측정 가능 | 모호함 |

---

## EXECUTOR에게 전달하는 형식

```
[EXECUTOR 지시]
- 작업 범위: {파일 목록}
- 준수할 컨벤션: docs/CONVENTIONS.md 참조
- 금지 패턴: docs/DO_NOT.md 참조
- 완료 후 REVIEWER에게 품질 검토 요청
```
